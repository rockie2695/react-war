import { Link } from "react-router-dom";

export default function BB() {
  return (
    <header>
      <nav>
        <Link to="/aa">aa</Link>
        <Link to="/bb">bb</Link>
      </nav>
    </header>
  );
}
